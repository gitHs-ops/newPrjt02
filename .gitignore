# 실제 배포용 프록시 — API 키·시트 ID 가 들어간다.
# 저장소에는 tools/career_proxy.example.gs 만 둔다.
career_proxy.gs
tools/career_proxy.gs

# 로컬 전용 설정 — 배포된 /exec URL 한 줄. `.\init.ps1 -Live` 가 읽는다.
local.endpoint.txt

# 저장된 상담 보고서(학생 개인정보) — 저장소에 들어오면 안 된다
*.report.md
reports/

# 파이썬
__pycache__/
*.pyc

# 편집기·OS
.DS_Store
Thumbs.db
*.swp
